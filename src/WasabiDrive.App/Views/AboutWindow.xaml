<Window x:Class="WasabiDrive.App.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="About WasabiDrive" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="20">
        <TextBlock Text="WasabiDrive" FontSize="20" FontWeight="Bold"/>
        <TextBlock x:Name="VersionText" Margin="0,4,0,0" Foreground="#555"/>

        <TextBlock Margin="0,14,0,0" TextWrapping="Wrap"
                   Text="Mount Wasabi S3 buckets as native Windows drive letters."/>

        <Separator Margin="0,14"/>

        <StackPanel Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Publisher: " FontWeight="SemiBold"/>
            <TextBlock Text="RHC Solutions"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Website: " FontWeight="SemiBold"/>
            <TextBlock><Hyperlink Click="OnOpenWebsite">https://rhcsolutions.com/</Hyperlink></TextBlock>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Source: " FontWeight="SemiBold"/>
            <TextBlock><Hyperlink Click="OnOpenGitHub">github.com/RHC-Solutions/WasabiDrive</Hyperlink></TextBlock>
        </StackPanel>

        <TextBlock x:Name="CopyrightText" Margin="0,14,0,0" Foreground="#777" FontSize="11"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Check for updates" Click="OnCheckForUpdates" x:Name="CheckButton"
                    Padding="14,6" Margin="0,0,8,0"/>
            <Button Content="Close" IsCancel="True" IsDefault="True" Padding="18,6"/>
        </StackPanel>
    </StackPanel>
</Window>
